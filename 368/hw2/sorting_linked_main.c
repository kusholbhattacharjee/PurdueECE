
/*
int main(){
	
	Gap* g = getGapArray(15);
	while(g!=NULL){
		printf("%d ",g->value);
		g = g->next;
	}
	Node *list = Load_From_File("15.b");
	Node *temp = list;
	Node *head = list;
	while(temp != NULL){
		printf("%ld ",temp->value);
		temp = temp->next;
	}
	head = Shell_Sort(head);
	printf("\n");
	while(head != NULL){
		printf("%ld ",head->value);
		head = head->next;
	}
	return 0;
}*/
